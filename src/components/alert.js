import React from 'react'
import Link from 'gatsby-link'

const Alert = ({ alertContent }) => (
  <section name="alert" className="alert">
    <p>{alertContent}</p>
  </section>
)

export default Alert
